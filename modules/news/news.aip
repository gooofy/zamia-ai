% prolog

train_prefix('{self_address:L} ').

train(en) :- or(
                "what is happening today",
                "what's going on"
               ),
             "Sorry, my news module is not implemented yet.".
train(de) :- or(
                "was ist heute los",
                "was gibt es"
               ),
             "Tut mir leid, mein Nachrichtenmodul ist noch nicht implementiert.".

